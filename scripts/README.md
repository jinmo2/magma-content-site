# scripts/ — TODO 슬롯

지금은 `preflight.mjs`만 들어 있습니다.

섹션 6.2 「콘텐츠 백엔드 — oh-my-wiki 활용」 실습에서 oh-my-wiki의 지식을
`content/posts/` 마크다운으로 내보내는 파이프 스크립트가 이 자리에 추가됩니다.

내보내는 파일은 `AGENTS.md`의 frontmatter 계약을 따라야 합니다.
실적 리포트를 내보내는 스크립트는 `content/reports/`와 `collection: "reports"` 계약을 사용합니다.
